import React from 'react';

import { StyledHeader } from "components/Header/components";

function Header() {

  return (
    <StyledHeader>
      Calculator App
    </StyledHeader>
  );
}

export default Header;
